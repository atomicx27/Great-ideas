# Automated-Resume-Filter

A classic **Automation** tool for the Human Resources domain. It uses deterministic keyword-matching logic to filter incoming resumes. It evaluates the presence of required keywords and automatically calculates a match score, saving recruiters time on initial resume screening without relying on complex AI.
