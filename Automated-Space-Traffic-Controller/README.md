# Automated Space Traffic Controller
A deterministic automation tool that checks satellite coordinates and warns of potential collisions based on predefined distance thresholds. It uses predefined logic and no AI.
